# Memory

## Promoted


## Staged

- [id:000001 | 2026-08-02 | src:human | uses:0] Bridge live write path verified end-to-end 2026-08-02
- [id:7ba7b8 | 2026-08-02 | src:cloud | uses:0] Hub endpoint gotcha: atlas_persona reports failures as soft ok:false dicts, and memory/soul have dedicated replace functions
- [id:8d071f | 2026-08-02 | src:local | uses:0] systemd user services and fresh terminals miss ~/.local/bin on PATH — pin the absolute path for the claude binary
- [id:6b9744 | 2026-08-02 | src:cloud | uses:0] Browser-seat loop test 2026-08-02: claude.ai/code can write lessons straight into the Atlas brain via INBOX.md
- [id:f5cab2 | 2026-08-02 | src:cloud | uses:0] Escalated Atlas sessions carry persona and memory context injected by the broker — verified 2026-08-02
- [id:befdfe | 2026-08-03 | src:cloud | uses:0] Claude-mode "no speech" in the GUI: the TTS pipeline (escalate -> /api/tts -> Audio.play) is verified working; the invisible causes are the persisted header mute (localStorage atlas-tts-enabled=false, easy to hit and forget) or a swallowed playTtsText rejection — both now print a status-line hint (commit 539aad2). Check the speaker icon in the header FIRST before debugging code.
- [id:f91966 | 2026-08-03 | src:cloud | uses:0] Electron shell "Claude doesn't speak" root cause chain: the desktop app has its OWN localStorage profile (~/.config/Atlas) — Chrome's Claude-mode selection does NOT carry over, and the app's long-lived window can run a days-stale bundle predating the Claude-voice wiring (BundleWatch's old speaking-latch bug suppressed auto-reloads). Diagnose from the hub log: voice input shows POST /api/stt/transcribe, escalation shows broker sessions, but a healthy voice reply MUST show POST /api/tts — its absence means the frontend never asked, i.e. stale bundle or wrong mode, not an audio problem. Ctrl+R the app and reselect the Claude agent in ITS Settings.
- [id:0e2169 | 2026-08-03 | src:cloud | uses:0] Ambient listening "drops after a few Q&As" had three roots (all fixed in one commit): (1) isTtsSpeaking was a counter and one missed decrement (playBlob play()-rejection) deafened the VAD permanently — speaking state must be DERIVED from live audio elements, never counted; (2) playTtsText audio was unstoppable, overlapping replies; (3) useMicStream never handled track.onended, so an OS-ended mic track (PipeWire suspend) killed listening silently — always re-acquire on track end. Pattern: any latch-able boolean that gates a loop WILL eventually latch; derive it from ground truth instead.
- [id:c1eb58 | 2026-08-03 | src:cloud | uses:0] Cross-window prefs sync shipped 2026-08-03: hub is source of truth via GET/PUT /api/prefs (hub/ui_prefs.py, per-key LWW so different-pref changes never clobber; allowlisted keys: chat_model, seat_model, tts_enabled, tts_voice, thinking_effort, gemini_google_search). Frontend prefsSync.ts pulls after agent bootstrap (fresh-profile defaults can never out-vote a deliberate choice), seeds missing keys once, pushes debounced. This closes the split-profile trap: Electron and Chrome now share agent selection, mute, and seat model. Rule learned: never push empty/unset values into a LWW store — '' out-voted a real choice once.
- [id:8f0cc8 | 2026-08-03 | src:cloud | uses:0] Seat hands v1 shipped 2026-08-03: escalated Claude now has atlas_launch_app, atlas_open_path (home-bounded), atlas_start_service (tile ids), atlas_projects (spine list/create/switch) — all MCP tools proxying the broker's whitelisted native bridge or hub REST, never raw shell. MISSION.md now injected into every escalation and carries the "Grow Atlas by building Atlas" ambition. Verified live: seat listed the real project spine. Remaining maker gap: no forge access — the seat cannot yet PROPOSE new tools into the approve flow; needs a hub REST create-batch endpoint + UI surfacing of out-of-band pending batches (forge batches are currently only born inside the local chat flow, app.py:1060).
- [id:f17daf | 2026-08-03 | src:cloud | uses:0] Forge access shipped 2026-08-03: seat proposes tools via MCP atlas_propose_tool → POST /api/forge_batch/propose (source-tagged, single tool allowed) → PendingForgeWatch polls /api/forge_batch/pending and opens the standard ForgeBatchModal → human approves → existing build pipeline. Proven in production the same night: seat proposed etna_quake_digest (INGV seismicity digest), Owen's window surfaced it, he approved live, Fable built it, sandbox passed. Found+fixed on the way: runner install_tool now heals the broken skip_pip promise (installs DECLARED requirements on ModuleNotFoundError and retries once — gate-safe since the declared list is what approval vetted). Still open: why the batch pipeline's pip-approval pause never fired for a tool with new requirements (requirements propagation suspect, build_pipeline.py:467).
- [id:c3988c | 2026-08-03 | src:cloud | uses:0] Viewer theming decision 2026-08-03 (Owen): TWO BRANDS, CLEANLY SPLIT — the public GeoID site keeps its v2 identity (one teal accent #35d6b8, near-black, Archivo/Inter, radius 2; rationale written in styles/v2-site.css) while viewers served inside Atlas tiles (:6082) load /styles/atlas-skin.css (indigo #0a0118, cyan #38e5ff, Inter/JetBrains Mono). Mechanism on both sides is font-remap + token-redefinition loaded LAST — never per-page rewrites. Site-side completion file is styles/v2-viewer.css (explorer/etna/myGeoID; synthwave skin dropped). Host has NO Inter installed (fc-list=0) — font remaps must use gstatic URLs like the hub does, never bare local(). Site repo commit NOT pushed: push publishes geoidinitiative.com, Owen's call.
- [id:10d24f | 2026-08-03 | src:cloud | uses:0] Tile embed URLs have FOUR copies and only ONE matters: hub /api/agents/<id>/live embed_url (env defaults ATLAS_*_EMBED in hub/app.py, BAKED — rebuild required) is what launchApp.ts actually embeds; the SERVICE_APPS urls in desktop/main.js and the broker are returned by startService but IGNORED for the embed. Repointing a tile = change the hub default (and bump its ?v= cache-buster to defeat Electron's HTTP cache). Cost a full debugging round on 2026-08-03 when only main.js+broker were repointed and "nothing changed".
- [id:3d1bb5 | 2026-08-03 | src:cloud | uses:0] Skin-strip disaster 2026-08-03, the rule learned: when theming embedded/forked pages, NEVER delete stylesheet links you didn't write — /styles/viewer-skin.css looked like a brand layer but was the viewers' FUNCTIONAL stylesheet (the site externalized viewer styling into central /styles/*), and stripping it shipped unstyled tiles with the flight sim UI invisible. Correct mechanism: sync the ENTIRE styles/ family under the serving root, keep every link the page ships with, and win by appending your token file LAST in the cascade. sync-viewers.sh now encodes this (append-only doctrine).
