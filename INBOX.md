# Inbox

Drop one lesson per `- ` bullet. The local sync loop ingests and
empties this file; provenance is added automatically.
- Hub endpoint gotcha: atlas_persona reports failures as soft ok:false dicts, and memory/soul have dedicated replace functions
